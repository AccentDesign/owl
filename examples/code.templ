package examples

import (
    "github.com/accentdesign/owl/components"
    "github.com/accentdesign/owl/css"
)

templ Code() {
    @Base() {
        @css.Include("code.css")

        <h2 class="owl-h2 mb-10">code</h2>

        <p class="mb-6">
        Some inline
        @components.Code("mx-1") {
            code
        }
        block.
        </p>

        @components.Pre("") {
            @components.Code("") {
                git clone https://github.com/AccentDesign/owl.git
            }
        }
    }
}