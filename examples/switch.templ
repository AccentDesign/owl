package examples

import (
    "github.com/accentdesign/owl/components"
    "github.com/accentdesign/owl/utils"
)

templ Switch() {
    @Base() {
        @utils.InlineCss("css/switch.css")

        <h2 class="owl-h2 mb-10">switch</h2>

        @components.Switch()
    }
}