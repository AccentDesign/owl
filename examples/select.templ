package examples

import (
    "github.com/accentdesign/owl/components"
    "github.com/accentdesign/owl/css"
)

templ Select() {
    @Base() {
        @css.Include("select.css")

        <h2 class="owl-h2 mb-10">select</h2>

        @components.Select()
    }
}